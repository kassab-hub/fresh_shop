import 'package:flutter/material.dart';

class AppColors {
  // اللون الأخضر العشبي باستخدام الـ Hex Code (#27AE60)
  static const Color primary = Color(0xFF27AE60);

  // اللون البرتقالي المشرق
  static const Color accent = Colors.orange;

  // خلفية التطبيق الفاتحة جداً لبروز المنتجات
  static const Color background = Color(0xFFF8F9FA);

  // نصوص التطبيق الداكنة
  static const Color textDark = Color(0xFF2D3436);
}
